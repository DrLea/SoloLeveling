// Public config — safe to commit. The OAuth Client ID is not a secret.
// Paste your Google OAuth Web Client ID here (see README), or enter it in the app.
window.SS_CONFIG = {
  googleClientId: ""
};
